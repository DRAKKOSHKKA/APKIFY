# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# GitHub Actions & GitHub CLI Capabilities

The GitHub CLI is installed and authenticated for `DRAKKOSHKKA` on this machine.
Executable path: `C:\Program Files\GitHub CLI\gh.exe`

Always use the following commands to check CI runs and fetch logs without asking the user:
- List recent runs:
  `& "C:\Program Files\GitHub CLI\gh.exe" run list --limit 5`
- View failed logs for a run:
  `& "C:\Program Files\GitHub CLI\gh.exe" run view <RUN_ID> --log-failed`
- Check specific run status:
  `& "C:\Program Files\GitHub CLI\gh.exe" run view <RUN_ID>`
- Watch running workflow until completion:
  `& "C:\Program Files\GitHub CLI\gh.exe" run watch <RUN_ID>`
